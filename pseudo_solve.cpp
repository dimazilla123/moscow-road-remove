#include <iostream>
#include <vector>
#include <algorithm>
#include <set>
#include <unordered_set>
#include <map>
#include <unordered_map>

#ifndef DEBUG
#define fprintf if (0) fprintf
#endif

int main()
{
    int n, m;
    std::cin >> n >> m;
    std::cout << 0 << "\n";
    for (int i = 1; i <= m; i++)
        std::cout << i << " ";

    return 0;
}

